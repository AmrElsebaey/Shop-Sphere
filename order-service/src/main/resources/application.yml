spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_service
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: none

server:
  port: 8081

inventory:
  url: http://localhost:8082